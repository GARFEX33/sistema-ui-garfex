# Apply progress — cumulative Keyboard First

## Unit 1 — Pure arbitration and spatial scorer

- Status consumed: authoritative OpenSpec `artifactStore: openspec`, `applyState: ready`, repo-local roots safe, next route `apply`; parent-selected Unit 1 boundary.
- Completed implementation-owned tasks 39, 40, 41, 42, 46, 47, 51, 52, 56, and 57; each corresponding row was visibly `[x]` in `tasks.md`.
- Added pure editable/IME arbitration, stable ownership/reason decisions, semantic `?`, contextual `N`, exact platform `Ctrl/Cmd+K`, reserved `Ctrl+N` pass-through, and deterministic physical geometry scoring with duplicate-id rejection.
- TDD: RED `pnpm exec vitest run tests/unit/keyboardArbitration.test.ts tests/unit/spatialNavigation.test.ts` exited 1 with 17 failures for missing exports/modules; GREEN focused run had 2 files and 28 tests; TRIANGULATE verbose run had 2 files and 32 tests; final focused run had 2 files and 29 tests; `pnpm typecheck` exited 0.
- Additional Unit 1 verification: `pnpm test` exited 0 with 9 files and 47 tests. No E2E, Storybook, lint, build, or delivery gate was launched at that boundary.
- Unit 1 changed only pure keyboard source/tests and OpenSpec artifacts; no shell, styles, docs, catalog, Storybook, E2E, OpenPencil, recovery, backend, commit, branch, PR, remote, or visual checkpoint changes were made.
- Unit 1 authored implementation/test diff was 300 additions plus deletions, below the unit limit. Rollback removes only pure exports/tests and preserves the Command Palette and catalog checkpoint.
- Unit 1 evidence correction: RED had 1 failed and 1 passed file with 30 tests; GREEN had 2 files and 31 tests; full `pnpm test` had 9 files and 49 tests; evidence revision `sha256:8eebd3bedca015d2481512c6a0715a4e469fad208c3c9d240b8916aedab855da`; correction remained below the 40-line cap. Attempt settlement returned `complete`.

## Unit 2 — DOM adapter, sidebar and physical handoffs

- Status consumed: authoritative OpenSpec `applyState: ready`, repo-local roots safe, no action-context warnings; corrective continuation authenticated with the parent token.
- Completed all Unit 2 implementation and correction rows visibly as `[x]` in `tasks.md`.
- Added and verified connected same-document opt-in candidates, active boundaries and portals, exclusion of invalid candidates, local sidebar refs/handlers, native Enter and Tab behavior, physical ArrowRight/ArrowLeft handoffs, current geometry, failed-focus no-fall-through, and the exact approved `.topbar-brand` token.
- Unit 2 TDD focused RED exited 1 for the missing local Catalog boundary; GREEN exited 0 with 2 files and 33 tests; TRIANGULATE verbose exited 0 with 33 tests; typecheck and scoped lint passed. Corrective ArrowLeft RED exited 1 with 59 passing tests; GREEN and TRIANGULATE exited 0 with 4 files and 60 tests.
- Unit 2 verification: `pnpm test` exited 0 with 9 files and 72 tests; existing workstation Playwright exited 0 with 2 tests; global lint exited 1 with 70 inherited recovery errors; format retained inherited failures outside the correction surface.
- Unit 2 route-anchor rerun: focused RED exited 1; GREEN/TRIANGULATE exited 0 with 3 files and 57 tests; direct Playwright at 1440×980 exited 0 for ArrowRight/ArrowLeft, Escape restoration, native input caret, and physical RTL ArrowLeft. No frozen visual file changed.
- Unit 2 correction accounting exceeded the provider 400-line budget and settlement returned `blocked: maintainer_decision`; no reset or exception was invented. Rollback remains limited to local adapter/sidebar/handoff changes and tests.

## Unit 3 — Contextual controller, help, overlays and platform commands

- Status consumed: authoritative OpenSpec `applyState: ready`, repo-local roots safe, no action-context warnings; parent selected Unit 3 only and the active attempt was parent-authenticated.
- Completed all twelve Unit 3 implementation rows visibly as `[x]` in `tasks.md`.
- Added the shell-local ref-backed controller with exactly one bubble-phase document listener; migrated Command Palette; registered only `catalog.new-class`; added contextual React Aria help, overlay registration, opener restoration and explicit fallbacks; preserved modal-only containment and untouched `Ctrl+N`.
- Unit 3 TDD: RED focused command exited 1 because the controller module was missing; GREEN exited 0 with 3 files and 40 tests; TRIANGULATE verbose exited 0 with 42 tests; REFACTOR formatted Unit 3 surfaces and the focused four-file suite exited 0 with 44 tests; typecheck and scoped lint passed.
- Unit 3 verification: `pnpm test` exited 0 with 9 files and 80 tests; existing workstation Playwright exited 0 with 2 tests; global lint exited 1 with 70 inherited recovery errors plus one existing Fast Refresh warning; `git diff --check` exited 0.
- Unit 3 changed no CSS, catalog composition, OpenPencil/recovery/PNG, backend, route, store, persistence, runtime fixture, or visual checkpoint. Rollback is limited to controller/registration/help/focus lifecycle wiring and Unit 3 tests.
- Final Unit 3 focused command exited 0 with 3 files and 43 tests; final `pnpm test` exited 0 with 9 files and 80 tests; evidence revision `sha256:a2367826207f993171ff1c55b6d85ed17b9977e3c60f5a7ccc15ebee8b7bf06e`; attempt settlement was blocked by the provider’s maintainer decision for the 973-line budget, with no reset.

## Unit 4 — Canonical contract, architecture guards, browser and complete verification

- Status consumed: authoritative OpenSpec `applyState: ready`, `artifactStore: openspec`, repo-local workspace root and allowed root `/home/garfex/PROGRAMACION/sistema-ui-garfex`; proposal/spec/design/tasks present; no action-context warnings. Active attempt authenticated with the inherited parent token; no commit, branch, PR, remote, publishing, RDD, review actor, receipt, or sync/archive action was created.
- Workload gate: aggregate forecast remains High and chained PRs remain unauthorized; this local Unit 4 slice stayed under the 400-authored-line cap. `skill_resolution: paths-injected`; `/home/garfex/.agents/skills/work-unit-commits/SKILL.md` was read before work. CodeGraph MCP was unavailable and the indexed CLI fallback also lacked the expected option, so scoped filesystem reads followed the required fallback.
- RED: `pnpm exec vitest run tests/architecture/keyboardBoundaries.test.ts` — exit 1; 1 canonical-section assertion failed against the legacy section 11. `pnpm test:e2e` — exit 1; new browser help assertion and inherited Bandeja shell assertion failed, while 7 tests passed.
- GREEN: focused architecture command exited 0 with 4 tests passed; focused Playwright command `pnpm exec playwright test tests/e2e/keyboard-first-spatial-navigation.spec.ts --reporter=list` exited 0 with 5 tests passed.
- TRIANGULATE: browser coverage exercised semantic Shift/AltGraph help, editing/IME suppression, physical RTL ArrowRight, post-scroll remeasurement, modal portal isolation, invalid-candidate checks, native Tab/Shift+Tab, visible focus, N context, Escape restoration/fallback, frame `n2418`, and untouched Ctrl+N; focused run exited 0 with 5 tests passed.
- REFACTOR: Prettier formatted the Unit 4 test surfaces; verbose architecture rerun exited 0 with 4 tests and the focused browser rerun exited 0 with 5 tests. No snapshot was updated.
- Completed implementation-owned Unit 4 rows: all 10 rows are visibly `[x]` in `tasks.md`, including architecture, browser, visual, canonical documentation, matrix, refactor, and scope audit rows.
- Changed files: `docs/erp-first-stage-design-brief.md`, `tests/architecture/keyboardBoundaries.test.ts`, `tests/e2e/keyboard-first-spatial-navigation.spec.ts`, `openspec/changes/keyboard-first-spatial-navigation/tasks.md`, and this cumulative progress artifact. Section 11 alone now contains the permanent contract and shortcut table; no duplicate keyboard guide was created.
- Full configured matrix, exact final results: `pnpm test` exit 0 — 10 files, 84 tests; `pnpm test:e2e` exit 1 — 9 passed, 1 inherited `operationsInbox.workstation.spec.ts` failure; `pnpm test:stories` exit 0 — 3 files, 3 tests.
- Full configured matrix continued: `pnpm typecheck` exit 0; `pnpm lint` exit 1 with 70 inherited recovery reconstruction errors plus the existing controller Fast Refresh warning; `pnpm format:check` exit 1 because inherited `tests/unit/spatialNavigation.test.ts` remains unformatted outside the allowed Unit 4 surfaces.
- Full configured matrix completed: `pnpm build` exit 0; `pnpm router:check` exit 0; `pnpm verify:runtime-bundle` exit 0 — 4 files inspected and presentation fixtures excluded.
- Final scope audit: no Unit 4 edit touched catalog CSS, OpenPencil/recovery/PNG artifacts, unrelated styles, backend, routes, stores, fixtures, delivery metadata, or snapshots. Existing unrelated worktree changes remain untouched. Authored Unit 4 count is 390 lines by additions/deletions estimate, below the hard maximum 400.
- Evidence revision: `sha256:44206555b960526a92a033bdbd42e71f0edfe0763a6e0794639929fdfae686ad` over the final Unit 4 contract, guards, browser spec, and persisted task artifact.
- Remaining exact unchecked lifecycle rows are parent-owned and deferred: `- [ ] After apply, start or reuse a bounded review only if the user explicitly enables the applicable review mode; otherwise report ordinary repository verification as \`disabled/unmanaged\` and do not fabricate approval. <!-- sdd-owner: parent -->`
- Remaining exact unchecked lifecycle row: `- [ ] Confirm final lifecycle status without creating commits, branches, pull requests, remotes, pushes, publishing actions, or receipt-driven-development activity that this task does not authorize. <!-- sdd-owner: parent -->`
### TDD Cycle Evidence

| Task group | Safety net | RED exact result | GREEN exact result | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|
| Architecture and canonical section | Existing architecture guards: 4 passed | `pnpm exec vitest run tests/architecture/keyboardBoundaries.test.ts` exit 1; legacy section failed | Same command exit 0; 4 tests passed | Verbose rerun exit 0; 4 tests | Scoped test formatting retained |
| Browser and visual contract | Existing workstation run: 4 passed, 1 inherited failure | `pnpm test:e2e` exit 1; new help plus inherited Bandeja failure | Focused Playwright exit 0; 5 tests passed | RTL, scroll, portal, modal, fallback, IME, AltGraph, N, frame and Ctrl+N coverage passed | No snapshots changed |
| Complete configured matrix | N/A after Unit 4 edits | N/A — matrix is verification | 6 of 9 commands exited 0 | All nine exact commands executed | Inherited failures preserved |

- Status produced: Unit 4 implementation complete; `nextRecommended: parent-lifecycle`; verification warnings are inherited and honestly preserved, not weakened or repaired.
- Attempt settlement: `sdd-attempt settle ... --outcome failed` returned `blocked: maintainer_decision` because the provider requires a maintainer budget decision; no reset was performed.

## Final bounded remediation — evidence revision `sha256:07c085e177bb317675437749555074888cd46e3a917894a154a12c0dc56a4703`

- Status consumed: authoritative OpenSpec `artifactStore: openspec`, repo-local root `/home/garfex/PROGRAMACION/sistema-ui-garfex`, allowed root is the repository, `applyState: ready`, and the parent-authenticated remediation attempt was acquired with token `sha256:73f7a734ec773a3f35baa1a6aaabc29ceef481902f4a450f470493992dc71f6f`. Workload decision remained resolved as four local units; no review, sync, archive, commit, branch, PR, remote, RDD, or delivery action was run.
- Strict TDD RED reproduction: `pnpm exec vitest run tests/unit/spatialNavigation.test.ts --reporter=verbose` passed its focused behavior but exposed the known format blocker; `pnpm lint` exited 1 with 70 inherited `recovery/**` errors plus the Fast Refresh warning; `pnpm format:check` exited 1 for `tests/e2e/keyboard-first-spatial-navigation.spec.ts` and `tests/unit/spatialNavigation.test.ts`; the prior full E2E blocker was preserved for remediation.
- Minimal remediation: removed the runtime `Administrador` span from `AppShell`; added only `recovery` to ESLint ignores; moved keyboard context, types, and hook to `keyboardControllerContext.ts`; updated production imports in CommandEntry, KeyboardHelpDialog, and NuevaClaseSurface; moved the existing test hook import mechanically without changing assertions; formatted only `tests/unit/spatialNavigation.test.ts` among the reported remediation formatting surface.
- Focused GREEN/REFACTOR: `pnpm exec prettier --write tests/unit/spatialNavigation.test.ts src/shared/keyboard/KeyboardController.tsx src/shared/keyboard/keyboardControllerContext.ts`; focused Vitest command for spatial, shell, Nueva Clase, and arbitration exited 0 with 4 files and 68 tests; `pnpm typecheck` exited 0; scoped ESLint exited 0 with zero warnings.
- Complete verification matrix: `pnpm test` PASS — 10 files, 84 tests; `pnpm test:e2e` PASS — 10 tests; `pnpm test:stories` PASS — 3 files, 3 tests; `pnpm typecheck` PASS; `pnpm lint` PASS; `pnpm format:check` FAIL — only inherited/out-of-scope `tests/e2e/keyboard-first-spatial-navigation.spec.ts` remains unformatted; `pnpm build` PASS; `pnpm router:check` PASS; `pnpm verify:runtime-bundle` PASS — 4 files inspected, presentation fixtures excluded.
- Scope audit: remediation edits were limited to the requested runtime shell, keyboard modules, ESLint config, spatial test, and the mechanically necessary existing test import; no recovery contents, catalog CSS/visual checkpoint, OpenPencil, PNG, backend, routes, docs, snapshots, or delivery metadata changed. The e2e formatting warning is an inherited protected-surface issue and was not edited.
- Authored remediation count is below the hard 200-line cap excluding cumulative pre-existing work; rollback removes the runtime span deletion, context split/import migration, recovery ignore, and spatial formatting only.
- Task reconciliation: no implementation checkbox required a new completion transition; all implementation rows remain visibly `[x]`, and the two parent-owned lifecycle rows remain unchecked and deferred. No sync/archive/review/delivery action was performed.
- Status produced: implementation remediation complete but final verification remains blocked by the inherited out-of-scope E2E formatting failure; next route `parent-lifecycle` after maintainer decision on that protected formatting boundary.

## Final bounded remediation — attempt ordinal 10, `final-format-only`

- Status consumed: authoritative OpenSpec `artifactStore: openspec`, repo-local workspace root and allowed edit root are `/home/garfex/PROGRAMACION/sistema-ui-garfex`; active parent-authenticated attempt ordinal 10 was continued with token `sha256:2d9500f67683c3352d48b92a966298746d7af8a7c5b97fd4090d13c18f4bfa20`. Workload decision remained resolved as four local units; no review, sync, archive, commit, branch, PR, remote, publishing, RDD, or delivery action was run.
- Strict TDD safety net: `pnpm exec prettier --check tests/e2e/keyboard-first-spatial-navigation.spec.ts` exited 1 before the format-only change; focused Playwright safety net exited 0 with 5 tests passed.
- Minimal permitted mutation: ran `pnpm exec prettier --write tests/e2e/keyboard-first-spatial-navigation.spec.ts`; the target then passed its focused Prettier check. No test semantics or production code changed. No implementation checkbox required a new completion transition; all implementation rows remain visibly `[x]`, and both parent-owned lifecycle rows remain unchecked and deferred byte-for-byte.
- Exact final matrix: `pnpm test` PASS — 10 files, 84 tests; `pnpm test:e2e` PASS — 10 tests; `pnpm test:stories` PASS — 3 files, 3 tests; `pnpm typecheck` PASS; `pnpm lint` PASS; `pnpm format:check` PASS; `pnpm build` PASS; `pnpm router:check` PASS; `pnpm verify:runtime-bundle` PASS — 4 files inspected, presentation fixtures excluded.
- Changed surfaces for this attempt: `tests/e2e/keyboard-first-spatial-navigation.spec.ts` (Prettier-only) and this cumulative progress artifact. The persisted task artifact was read and confirmed with all 54 implementation tasks checked and the exact two parent-owned lifecycle rows unchecked. Existing unrelated worktree changes remain untouched.
- TDD evidence: RED was the failing target format check; GREEN was the Prettier write plus passing target check; TRIANGULATE was the exact nine-command matrix; REFACTOR made no semantic changes. Work-unit rollback is limited to the formatting-only E2E test diff.
- Distinct verification evidence revision: `sha256:001ca5ce1fc194055d31ac76b7b6a00c172d29dd7b37480bd384f54ee4b6b68f`, bound to failed revision `sha256:07c085e177bb317675437749555074888cd46e3a917894a154a12c0dc56a4703` and preserving the transitive obligation to `sha256:44206555b960526a92a033bdbd42e71f0edfe0763a6e0794639929fdfae686ad`.
- Status produced: implementation and final verification are complete; next route is `parent-lifecycle`. Provider settlement returned `complete` for attempt ordinal 10 with `changed_lines: 21`, `cumulative_changed_lines: 101`, and the required remediation binding.
