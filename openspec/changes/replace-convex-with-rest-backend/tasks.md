# Tareas — Sustituir Convex por backend REST

Esta planificación migra el frontend sólo contra el contrato REST público ya aportado. No autoriza cambios, inspección ni pruebas mutantes del backend; una capacidad pública realmente insuficiente se detiene y se comunica en un reporte de brecha, nunca se emula en frontend.

## Review Workload Forecast

| Field                   | Value                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| ----------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Estimated changed lines | 6.696–8.336 líneas autorales totales (suma de slices, incluidas Unidad 6 lower-level, verificación efectiva 7a-i–7a-ii, 7b–7c, bases opcionales 7d–7e, evaluación diferida 7f, la remediación 11a y las dos unidades 12; adiciones + eliminaciones, pruebas incluidas)                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 400-line budget risk    | High                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Chained PRs recommended | Yes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| Suggested split         | PR 1 contratos/frontera → PR 2 configuración → PR 3 Clase → PR 4 lecturas dependientes → PR 5 creación Clase/adaptación G2-G3 (426, excepción aceptada) → PR 6a adapter APLICABILIDAD lower-level (completado) → PR 6b-i tipos/GET efectivo retenido → PR 6b-ii pruebas/guardas/verificación → PR 6c hook efectivo → PR 6d UI/wiring efectivo → PR 6e pulido lista/Dialog GARFEX → PR 7a base OPCION opcional → PR 7b base characteristicCode opcional → PR 7c evaluación Creador diferida → PR 8 adapter recursos → PR 9 bloqueo Creador G4-G5 → PR 10 jerarquía REST → PR 11 ventana REST aislada → PR 12 remediación residual del Creador → PR 13 extensión compartida HierarchyNavigator → PR 14 switch atómico de pantalla/proyección G8 → PR 15 mutaciones → PR 16 limpieza diferida de Convex |
| Delivery strategy       | auto-chain                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| Chain strategy          | stacked-to-main                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      |

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: stacked-to-main
400-line budget risk: High

**Gate de entrega:** el humano seleccionó entrega encadenada `stacked-to-main` bajo `ask-on-risk`; por ello la estrategia operativa es `auto-chain`. Esta decisión sólo planifica implementación/entrega: no autoriza commits, ramas ni PRs, y la entrega ordinaria requiere una solicitud explícita posterior del humano. Las estimaciones de cada unidad se vuelven a medir con `git diff --stat` antes de preparar cualquier PR; pruebas y documentación de la unidad viajan con el código. La unidad 12b se detiene bajo `ask-on-risk` antes de apply si su forecast medido supera 400 líneas: no absorbe la extensión compartida ni usa una excepción de tamaño sin aceptación humana explícita.

## Límites, evidencia y orden seguro

- La evidencia pública completa recibida ya permite validar las once variantes de `CatalogValue`: `TEXT`, `CODE`, `BOOLEAN`, `INTEGER`, `DECIMAL`, `QUANTITY`, `REFERENCE`, `ENUM`, `STRING_LIST`, `CONTROLLED_OPTION` y `NOT_APPLICABLE`, además de referencias y CRUD genérico de catálogo/recursos. G7 deja de bloquear el schema base; sigue prohibido inventar cualquier campo, ruta, status o semántica que no esté en OpenAPI.
- Sólo pueden emitir HTTP `src/features/catalog-hierarchy/catalogHierarchy.api.ts`, `src/features/catalog-hierarchy/catalogTypeAttributesRead.api.ts`, `src/features/catalog-hierarchy/catalogTypeEffectiveAttributes.api.ts` y `src/features/resources-master/resourcesMaster.api.ts`. `src/features/resources-master/resourceAttributeEvaluation.api.ts` sólo se vuelve un quinto adapter autorizado dentro de la unidad diferida 7f, tras confirmar la ruta pública y ampliar la guarda exacta. `catalogTypeAttributes.api.ts` legacy no es fallback ni ruta alternativa. Los tests inyectan `fetch`; no hay cliente global, fallback Convex, mock runtime, storage ni doble escritura.
- Las pruebas conectadas futuras son exclusivamente lecturas públicas desde navegador por el proxy Vite. Quedan prohibidas las pruebas conectadas que creen, actualicen, activen, desactiven o reenvíen mutaciones, salvo autorización explícita posterior del humano.
- Hay conflictos activos con `catalog-hierarchy-base` (pantallas, tipos, controlador y pruebas de catálogo) y `keyboard-first-resource-creation` (shell, hooks, modelo y pruebas del Creador). Antes de cada unidad compartida, releer los bytes y la aceptación vigente de esos changes, coordinar ownership con el parent y detener la unidad ante una edición incompatible. No modificar sus artefactos ni marcarlos como completados. La reconciliación histórica de `openspec/config.yaml` y de `adopt-query-zod` corresponde al parent; esta change sólo adapta sus guardas de forma explícita.

## Registro de gates de brecha

| Gate | Estado planificado y evidencia requerida para continuar                                                                                                                                                                                                                |
| ---- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| G1   | **Resuelto por evidencia pública ya capturada:** las lecturas dependientes aceptan `classCode` para `FAMILIA` y `familyCode` para `TIPO`; validar la referencia devuelta y no filtrar una colección global.                                                            |
| G2   | Sin orden estable público: no acumular/deduplicar ni prometer recorrido exhaustivo entre ventanas.                                                                                                                                                                     |
| G3   | **Cerrado de forma limitada para lectura efectiva:** Core documenta `effectiveMode`, `source`, reglas, participación de identidad, `notApplicable` y posición; `effectiveReasons`, agregados, violaciones y clasificación siguen bloqueados; la evaluación interactiva queda diferida y aislada en 7f.   |
| G4   | **Cierre limitado y diferido:** `POST /evaluate` respalda evaluación de valores por Core, pero su adapter/hook del Creador queda en unidad 7f independiente; fingerprint, disposiciones, incidencias y creación continúan bloqueados y no se emulan. |
| G5   | **Decisión humana:** bloquear temporalmente y de forma accesible selector/confirmación de unidad natural; no inferir el vínculo `UNIDAD` → `naturalUnit`.                                                                                                              |
| G6   | **Cerrado de forma limitada para lecturas:** Unidad 6 conserva `APLICABILIDAD` lower-level y el endpoint efectivo respalda la composición de pantalla; CRUD, lifecycle, allowed-values, `RELACION_OPCIONES` y los 14 métodos legacy permanecen bloqueados; la evaluación wire de 7f no demuestra equivalencia con ellos. |
| G7   | Extracto OpenAPI completo recibido: implementar schemas exactos de valores/referencias/rutas conocidas; reportar cualquier hueco residual, sin aceptar `unknown` como DTO válido.                                                                                      |
| G8   | **Decisión humana:** adaptar la UI de Recursos a campos REST reales; no mostrar nombre, ownership ni diagnóstico legacy inventados.                                                                                                                                    |

La evidencia pública ya capturada de G1 se aplica en la unidad 4 y no requiere reporte de brecha. G4, G5 y G8 tienen decisión humana registrada; sus registros sólo documentan el bloqueo o adaptación acordados. Cada reporte de gate no resuelto se escribe en `openspec/changes/replace-convex-with-rest-backend/gap-reports/G<n>.md` con operación usuaria, cita de evidencia pública revisada, impacto visible/operativo, petición HTTP que **no** se realizará y decisión humana requerida. Un reporte no solicita ni describe internals del backend.

## Unidades de implementación

### 1. Contrato REST de catálogo y frontera permitida — 300–380 líneas

**Depende de:** decisión de entrega antes de apply. **Finaliza cuando:** los DTOs públicos de catálogo se validan antes de React y las guardas limitan el transporte. **Rollback:** revertir sólo `src/shared/catalog/catalogRest.contract.ts`, `src/shared/api/restFailure.ts`, los tres adapters y sus pruebas de frontera; no tocar pantallas.

- [x] **RED → GREEN → TRIANGULATE → REFACTOR:** en `tests/unit/catalogRestContract.test.ts` y `tests/architecture/restTransportBoundaries.test.ts`, escribir primero casos rojos para records/páginas válidos e inválidos, IDs y revisiones `string`, envelope `{ error }`, segmentos codificados y las once variantes de `CatalogValue` (incluidas referencias); implementar los schemas Zod puros en `src/shared/catalog/catalogRest.contract.ts`, el fallo discriminado mínimo en `src/shared/api/restFailure.ts` y la whitelist AST de `fetch` para los tres adapters; triangular cada variante/rechazo sin coerción, trim, defaults ni valores parciales; refactorizar sólo duplicación contractual real y ejecutar `pnpm test`. <!-- sdd-owner: implementation -->

### 2. Configuración local, actor y proxy de desarrollo — 180–240 líneas

**Depende de:** unidad 1. **Finaliza cuando:** `/v1` (read-only) conserva el mismo origen en desarrollo, y cada mutación falla antes de `fetch` si el actor no es un string no vacío/no whitespace. **Rollback:** revertir `vite.config.ts`, la declaración Vite y los tests asociados; no modificar entorno real ni backend.

- [x] **RED → GREEN → TRIANGULATE → REFACTOR:** crear casos rojos en `tests/unit/restActorConfiguration.test.ts` y `tests/unit/restProxyConfiguration.test.ts` para `VITE_REST_ACTOR` ausente, vacío, whitespace, override de test y proxy `/v1 → http://localhost:8090` (read-only) sin rewrite; implementar el lector local mínimo en los tres `*.api.ts`, tipar `VITE_REST_ACTOR` en `src/vite-env.d.ts` si falta y configurar `server.proxy` para la ruta pública `/v1` (read-only) en `vite.config.ts`; triangular invocación directa de create/update/lifecycle para demostrar cero requests mutantes sin actor y que lecturas no llevan actor; refactorizar la configuración sin storage/logs/secretos y ejecutar `pnpm test`. <!-- sdd-owner: implementation -->

### 3. Catálogo: lectura REST de Clase y ventanas independientes — 300–390 líneas

**Depende de:** unidades 1–2. **Finaliza cuando:** Clase usa `GET` sobre `/v1/catalog/CLASE` (read-only) con `scope`, `text`, `limit`, `offset`, valida la página y no conserva cursor Convex. **Rollback:** revertir `catalogHierarchy.api.ts`, `catalogHierarchy.types.ts`, `catalogHierarchyState.ts`, `useCatalogList.ts` y sus tests; no alterar Familias/Tipos.

- [x] **RED → GREEN → TRIANGULATE → REFACTOR:** convertir primero en rojos `tests/unit/catalogHierarchyApi.test.ts`, `tests/unit/useCatalogList.test.ts` y `tests/unit/catalogHierarchyState.test.tsx` para URL/query whitelist, offset/reset, `hasPrevious`/`hasNext`, respuesta inválida/error HTTP y revisión string; sustituir en `src/features/catalog-hierarchy/catalogHierarchy.api.ts` el seam Convex por `createCatalogHierarchyRestApi(fetch)` y en `catalogHierarchy.types.ts`, `catalogHierarchyState.ts` y `useCatalogList.ts` el cursor por ventana offset feature-local; triangular cambios rápidos de filtro/respuesta stale y página vacía/error sin cursor sintético ni dedupe; refactorizar sin reescribir `src/shared/hierarchy/parentGatedListController.ts`, verificar `pnpm test` y registrar el comando/resultado. <!-- sdd-owner: implementation -->

### 4. Catálogo: lecturas REST dependientes por código — 300–390 líneas

**Depende de:** unidad 3 y coordinación con `catalog-hierarchy-base`. **Finaliza cuando:** `FAMILIA` se lee mediante `GET` sobre `/v1/catalog/FAMILIA` (read-only) acotado por el `classCode` seleccionado y `TIPO` mediante `GET` sobre `/v1/catalog/TIPO` (read-only) acotado por el `familyCode` seleccionado; las referencias devueltas validan el padre, cada cambio de padre limpia descendientes y reinicia offset, y no se fabrica cursor ni garantía de orden. **Rollback:** revertir `catalogHierarchy.api.ts`, `catalogHierarchy.types.ts`, `catalogHierarchyState.ts`, `useCatalogList.ts`, `CatalogHierarchyScreen.tsx`/`HierarchyNavigator.tsx` sólo si fueron necesarios y sus pruebas; no tocar creación de Clase.

- [x] **RED → GREEN → TRIANGULATE → REFACTOR:** añadir primero casos rojos en `tests/unit/catalogHierarchyApi.test.ts`, `tests/unit/useCatalogList.test.ts`, `tests/unit/catalogHierarchyState.test.tsx`, `tests/unit/catalogHierarchyScreen.test.tsx` y `tests/unit/catalogHierarchyKeyboard.test.tsx` para `classCode` en la lectura de `FAMILIA`, `familyCode` en la de `TIPO`, referencia `class`/`family` válida y coincidente con el padre seleccionado, respuesta inválida antes de React, cambio de Clase/Familia que limpia descendientes y reinicia offset, y `hasPrevious`/`hasNext` sin cursor/dedupe; implementar el transporte y mappers mínimos en `catalogHierarchy.api.ts`, tipos/estado en `catalogHierarchy.types.ts` y `catalogHierarchyState.ts`, secuencia offset en `useCatalogList.ts` y consumo necesario en `CatalogHierarchyScreen.tsx`; triangular padre cambiado durante petición, referencia ajena, página vacía/error y navegación de ventana sin afirmar orden estable; refactorizar sin filtro local global ni modificación de `parentGatedListController.ts`, y ejecutar `pnpm test`. <!-- sdd-owner: implementation -->

### 5. Catálogo: creación de Clase y adaptación visible G2/G3 — 426 líneas verificadas

**Depende de:** unidad 4 y coordinación con `catalog-hierarchy-base`. **Finaliza cuando:** sólo Clase crea con actor y `code`, `name`, `plural`, `slug`; la UI no promete continuidad ordenada entre ventanas (G2) ni presenta `effective`, razones, agregados o violaciones como resultados REST (G3). **Rollback:** revertir `CatalogHierarchyScreen.tsx`, `NuevaClaseSurface.tsx`, `HierarchyNavigator.tsx` sólo si fue necesario, adapter/tipos y tests de esta unidad.

**Excepción de tamaño:** el humano aceptó explícitamente el corte verificado de 426 líneas para esta unidad; no modifica `auto-chain` ni autoriza excepciones para unidades posteriores.

- [x] **RED → GREEN → TRIANGULATE → REFACTOR:** añadir primero casos rojos en `tests/unit/catalogHierarchyCreation.test.tsx`, `tests/unit/catalogHierarchyNewClass.test.tsx`, `tests/unit/catalogHierarchyKeyboard.test.tsx` y `tests/e2e/catalogHierarchy.workstation.spec.ts` para payload de Clase, actor ausente, foco/Enter/Escape, plural/slug explícitos, copy accesible de alcance de orden no garantizado y ausencia de indicadores G3; adaptar `NuevaClaseSurface.tsx`, `CatalogHierarchyScreen.tsx`, `catalogHierarchy.api.ts` y tipos para el create genérico documentado y la adaptación visible; triangular 201 válido, 409/422/503, respuesta 201 inválida, cambio de padre y retorno de foco; retirar/adaptar `effective`, razones, agregados y violaciones sin cálculo local, registrar G2/G3 en `gap-reports/G2.md` y `G3.md` con evidencia/impacto/cálculo no realizado, preservar `Field`/`Dialog`/tokens existentes y ejecutar `pnpm test`. <!-- sdd-owner: implementation -->

### 6. Atributos directos: adapter REST de `APLICABILIDAD` por `typeCode` — 340–390 líneas

**Depende de:** unidades 1–4 y coordinación con `catalog-hierarchy-base`. **Finaliza cuando:** el nuevo adapter feature-local de sólo lectura solicita exclusivamente `GET /v1/catalog/APLICABILIDAD` con `typeCode`, `offset` y `limit`, valida la página completa antes de React contra el snapshot Clase→Familia→Tipo y entrega sólo DTOs directos. **Rollback:** revertir sólo `src/features/catalog-hierarchy/catalogTypeAttributesRead.types.ts`, `src/features/catalog-hierarchy/catalogTypeAttributesRead.api.ts`, las guardas exactas de transporte y sus pruebas; no modificar pantalla, hooks, superficies ni la API legacy.

**Corte vigente de G6:** esta unidad reemplaza el bloqueo total histórico de las unidades 6–7 con una lectura directa respaldada. No migra `catalogTypeAttributes.api.ts`, CRUD, lifecycle, `RELACION_OPCIONES`, allowed-values, herencia ni semánticas `effective`; esos recorridos siguen bloqueados y no se emite request exploratoria.

- [x] **RED → GREEN → TRIANGULATE → REFACTOR:** crear primero `tests/unit/catalogTypeAttributesReadContract.test.ts` y `tests/unit/catalogTypeAttributesReadApi.test.ts` como casos rojos para `DirectApplicabilityRequest` con snapshot completo, URL codificada que envía sólo `typeCode`/`offset`/`limit`, página `APLICABILIDAD` válida y flags REST, y rechazo de envelope, kind, `rules`, modo, id/revisión no string, `class`/`family`/`characteristic` no-REFERENCE o con kind/código discordante, `type` ausente, no-REFERENCE, con kind distinto de `TIPO` o cuyo código no coincide exactamente con el `typeCode` seleccionado y `optionSet` mal tipado; implementar los DTOs puros en `src/features/catalog-hierarchy/catalogTypeAttributesRead.types.ts` y `createCatalogTypeAttributesReadApi(fetch)` en `src/features/catalog-hierarchy/catalogTypeAttributesRead.api.ts`, validar toda la página y hacer fallar cerrado antes de React sin defaults, filtro local ni datos parciales; triangular id de referencia `"0"`, ausencia de `type` rechazada porque la vista directa por Tipo no admite records de nivel Familia, HTTP/red/JSON inválido y offset/flags; sustituir la whitelist exacta de HTTP en `tests/architecture/restTransportBoundaries.test.ts` sin permitir la API legacy como alternativa, refactorizar sólo duplicación contractual y ejecutar `pnpm test` y `pnpm typecheck`. <!-- sdd-owner: implementation -->

### 7a-i. Atributos efectivos: tipos estrictos y GET/parser retenido — 260–350 líneas

**Depende de:** unidades 1–4; Unidad 6 permanece lower-level y no es dependencia de composición. **Historial de partición:** el candidato U7a de 611 líneas quedó verde, se conserva para continuar y queda **reset** como entrega verificable; no se revierte ni se reimplementa para simular TDD. **Finaliza cuando:** el candidato retenido deja `catalogTypeEffectiveAttributes.types.ts` y `catalogTypeEffectiveAttributes.api.ts` con el GET efectivo, URL determinista, parser estricto y `EffectiveAttribute.options` requerido como `{ code, label }[]`, sin completar respuestas antiguas. **Rollback:** revertir sólo el corte retenido de tipos/adapter/parser; conservar Unidad 6 como historial/capacidad administrativa.

- [x] **RED → GREEN → TRIANGULATE → REFACTOR:** sobre el candidato retenido, fijar primero los casos rojos mínimos de `options` ausente, null/no-array, code/label no string, clave extra y elemento inválido entre válidos; ajustar exclusivamente `src/features/catalog-hierarchy/catalogTypeEffectiveAttributes.types.ts` y `src/features/catalog-hierarchy/catalogTypeEffectiveAttributes.api.ts` para exigir `options` en cada atributo, preservar su orden y rechazar la respuesta completa sin `options ?? []`, defaults, coerción ni filas parciales; conservar GET `GET /v1/types/{encodedTypeCode}/attributes/effective` con `classCode` antes de `familyCode`, typeCode contextual exacto, rules/CatalogValue/source/mode/position existentes y cero llamadas a Unidad 6, Convex, PRESENTACION u OPCION; triangular `options: []` válido y `CONDITIONAL` sin evaluación, refactorizar sólo el parser retenido y ejecutar la prueba focalizada disponible. <!-- sdd-owner: implementation -->

### 7a-ii. Atributos efectivos: pruebas, guardas y verificación completa — 300–390 líneas

**Depende de:** unidad 7a-i. **Finaliza cuando:** el candidato retenido se verifica como un contrato GET completo, incluidos options obligatorios, tests de arquitectura y comandos de calidad; sólo entonces habilita 7b. **Rollback:** revertir conjuntamente los ajustes de tests/guardas de 7a-i–7a-ii, sin devolver el candidato a una implementación distinta.

- [x] **RED → GREEN → TRIANGULATE → REFACTOR:** completar `tests/unit/catalogTypeEffectiveAttributesApi.test.ts` con URL codificada/orden query, contexto incompleto y cero HTTP, exclusión de offset/limit/characteristicCode, typeCode exacto, array vacío de atributos, todos los dominios anidados y los rechazos estrictos de `options`; actualizar `tests/architecture/restTransportBoundaries.test.ts` para autorizar exactamente `catalogTypeEffectiveAttributes.api.ts` como cuarto adapter y prohibir Convex, bases lower-level, POST/evaluate y rutas alternativas; triangular HTTP/red/JSON, objeto extra y respuesta parcial; ejecutar y registrar `pnpm test`, `pnpm typecheck`, `pnpm lint`, `pnpm format:check` y `pnpm build`, corregir sólo defectos de este corte y medir cada slice con `git diff --stat` para confirmar <=400. <!-- sdd-owner: implementation -->

### 7b. Atributos efectivos: hook contextual y estados remotos — 300–350 líneas

**Depende de:** unidad 7a-ii. **Finaliza cuando:** `useCatalogTypeEffectiveAttributes.ts` posee únicamente estado local `waiting-context`/`loading`/`ready`/`empty`/`error`, captura el snapshot Clase→Familia→Tipo y descarta cualquier éxito, error o finally stale sin fallback a Unidad 6 o Convex. **Rollback:** revertir sólo `src/features/catalog-hierarchy/useCatalogTypeEffectiveAttributes.ts` y `tests/unit/useCatalogTypeEffectiveAttributes.test.tsx`; conservar el adapter efectivo.

- [x] **RED → GREEN → TRIANGULATE → REFACTOR:** crear primero `tests/unit/useCatalogTypeEffectiveAttributes.test.tsx` para contexto incompleto sin request, reset atómico de respuesta/error/selección al cambiar Clase, Familia o Tipo, carga y vacío confirmado, error HTTP/red/schema sin datos previos, A→B→A, cambio de ancestro con mismo typeCode, abort/finally stale, retry explícito del contexto vigente y bloqueo de doble envío; implementar el hook feature-local con generación, AbortSignal e identidad de los tres códigos originales, sin Query, cache global, retry automático, paginación ni callbacks/API en identidad; triangular desmontaje y que una respuesta efectiva válida nunca desencadena bases o evaluación; refactorizar sin exponer semántica derivada y ejecutar `pnpm test` y `pnpm typecheck`. <!-- sdd-owner: implementation -->

### 7c. Atributos efectivos: UI read-only y switch atómico de pantalla — 360–400 líneas

**Depende de:** unidades 7a-i, 7a-ii y 7b, y coordinación con `catalog-hierarchy-base`. **Finaliza cuando:** `CatalogHierarchyScreen.tsx` compone atributos sólo con el hook/DTO efectivo de Core, sin llamadas Convex, API legacy ni Unidad 6; conserva el sistema de diseño, presenta source/effectiveMode/reglas y el orden recibido, y deja toda acción mutante o evaluación explícitamente bloqueada. **Rollback:** revertir conjuntamente `src/features/catalog-hierarchy/CatalogTypeEffectiveAttributes.tsx`, el wiring de `CatalogHierarchyScreen.tsx` y sus pruebas; conservar adapter y hook efectivos.

- [x] **RED → GREEN → TRIANGULATE → REFACTOR:** fijar casos rojos en `tests/unit/catalogTypeEffectiveAttributes.test.tsx`, `tests/unit/catalogHierarchyScreen.test.tsx` y `tests/unit/catalogHierarchyKeyboard.test.tsx` para título/ayuda «Atributos efectivos del Tipo»/«Resueltos por Core · Sólo lectura», nombre/código/tipo/dimensión, source y effectiveMode literales, rules sin editor, identityParticipates/notApplicable/optionSetCode, `options` code/label en orden Core sin selección implícita, array Core preservado sin sort/group/dedupe, position mostrada sólo con hasPosition y carga/vacío/error/retry accesibles; implementar `CatalogTypeEffectiveAttributes.tsx` y cablear atómicamente `CatalogHierarchyScreen.tsx` al hook efectivo, reutilizando componentes, tokens, foco y teclado GARFEX existentes sin CSS, Query ni listeners globales; retirar del árbol de pantalla llamadas/handlers Convex y legacy de efectividad, y mantener Asignar/Editar/lifecycle/Gestionar opciones deshabilitados con razón accesible sin ejecutar evaluación, allowed-values o mutaciones; triangular Escape/foco, source FAMILY/TYPE y CONDITIONAL sin motor local, refactorizar sin mezclar Unidad 6, `characteristicCode`, PRESENTACION u OPCION, y ejecutar `pnpm test` y `pnpm typecheck`. <!-- sdd-owner: implementation -->

### 7c-i. Pulido UI: lista compacta y detalle Dialog GARFEX — 360–400 líneas objetivo

**Depende de:** unidad 7c completada y coordinación con `catalog-hierarchy-base`. **Preflight de tamaño:** antes de aplicar, estimar con los destinos y pruebas de esta unidad; si el forecast supera 400 líneas, detenerse antes de editar y pedir división explícita lista→detalle bajo `ask-on-risk`. **Finaliza cuando:** `CatalogTypeEffectiveAttributes.tsx` preserva el hook/API efectivo y el orden Core, pero presenta una lista primaria compacta y un detalle read-only feature-local compuesto con el Dialog GARFEX existente; no hay JSON crudo, llamadas extra ni acciones de negocio. **Rollback:** revertir sólo `src/features/catalog-hierarchy/CatalogTypeEffectiveAttributes.tsx`, el nuevo detalle feature-local y las pruebas de esta unidad; conservar hook, adapter y pantalla efectiva.

- [x] **RED → GREEN → TRIANGULATE → REFACTOR:** hacer fallar primero `tests/unit/catalogTypeEffectiveAttributes.test.tsx`, `tests/unit/catalogHierarchyScreen.test.tsx` y `tests/unit/catalogHierarchyKeyboard.test.tsx` para lista que conserva el orden Core y muestra sólo nombre más código/tipo secundario cuando desambigua, un único Button «Ver detalle» con nombre contextual por fila, ausencia inline de mode/source/rules/options/posición y ninguna petición adicional; abrir el detalle y comprobar secciones legibles Resumen, Aplicabilidad, Opciones y Reglas, listas ordenadas code/label y pares etiqueta/valor para `CatalogValue` sin `<pre>`, `JSON.stringify` ni objetos crudos, `options: []` como baseline sin opciones y reglas sin evaluación; comprobar Dialog GARFEX con Cerrar/Escape, foco restaurado al opener o fallback accesible al invalidar contexto, cierre ante carga/error/cambio de Clase/Familia/Tipo, teclado operable y sin detalle stale; implementar sólo la composición feature-local `CatalogTypeEffectiveAttributeDetail.tsx` y el rediseño de `CatalogTypeEffectiveAttributes.tsx` usando `Dialog`/`DialogHeading`/`DialogContent`/`DialogActions`, `Button`, `WorkCard`, tokens y utilidades Tailwind responsivas existentes, sin CSS nuevo, valores arbitrarios ni componente shared; triangular source FAMILY/TYPE, hasPosition falso/verdadero, texto largo/viewport, `CONDITIONAL` sin motor local y que no hay POST/evaluate, allowed-values, mutaciones ni selección implícita; refactorizar sin tocar hook/API y ejecutar `pnpm test` y `pnpm typecheck`. <!-- sdd-owner: implementation -->

### 7d. Base opcional `OPCION` aislada de atributos efectivos — 250–330 líneas

**Depende de:** decisión de producto y de un `optionSetCode` explícitamente válido; no depende ni modifica el switch 7c. **Estado:** diferida; su salida no forma allowed-values ni evaluación. **Finaliza cuando:** existe sólo si el producto la requiere una lectura base independiente con el filtro público documentado, validación propia y estado separado, sin alimentar el endpoint efectivo. **Rollback:** revertir exclusivamente la base/hook/vista opcional y sus pruebas.

- [ ] **RED → GREEN → TRIANGULATE → REFACTOR:** tras decisión de producto, escribir primero `tests/unit/catalogOptionRead.test.ts` para omitir request sin optionSetCode válido, URL/DTO/referencias/offset/error/stale exactos y rechazo de respuesta parcial; añadir la lectura base al adapter lower-level autorizado `src/features/catalog-hierarchy/catalogTypeAttributesRead.api.ts` y sus tipos, con hook/vista propios sólo si fueron aprobados, sin importarla desde `CatalogTypeEffectiveAttributes.tsx`; triangular que no traduce OPCION a allowed-values, no evalúa reglas ni condiciona source/effectiveMode, no muta ni consulta `RELACION_OPCIONES`; conservar la whitelist sin otro adapter y ejecutar `pnpm test`. <!-- sdd-owner: implementation -->

### 7e. Base opcional por `characteristicCode` aislada de atributos efectivos — 250–330 líneas

**Depende de:** decisión de producto y de la ruta/DTO públicos exactos que documenten `characteristicCode`; no depende ni modifica el switch 7c. **Estado:** diferida; no sustituye ni filtra localmente el endpoint efectivo. **Finaliza cuando:** la evidencia en `openspec/changes/replace-convex-with-rest-backend/api-contract-evidence.md` identifica una lectura base concreta y la implementación aprobada conserva su validación y estado independientes. **Rollback:** revertir exclusivamente esa base opcional y sus pruebas.

- [ ] **RED → GREEN → TRIANGULATE → REFACTOR:** después de la decisión de producto y de confirmar en `api-contract-evidence.md` ruta, query, DTO y semántica de `characteristicCode`, crear pruebas rojas en `tests/unit/catalogCharacteristicCodeRead.test.ts` para URL codificada, validación total, error/stale y ausencia de composición; ampliar sólo el adapter lower-level autorizado `src/features/catalog-hierarchy/catalogTypeAttributesRead.api.ts` y sus tipos con la lectura confirmada, y crear hook/vista propios únicamente si fueron aprobados; triangular que la base no reconstruye, reemplaza ni ordena atributos efectivos, no evalúa condiciones ni habilita allowed-values o mutaciones; si falta evidencia pública exacta, registrar el bloqueo G6 sin enviar request exploratoria; ejecutar `pnpm test`. <!-- sdd-owner: implementation -->

### 7f. Creador: adapter y hook diferidos de `POST /evaluate` — 340–400 líneas

**Depende de:** decisión de retomar el Creador y confirmación pública de ruta absoluta/contexto de `POST /evaluate`; no depende de 7a-i/7a-ii ni bloquea el GET o la pestaña Catálogo. **Estado:** diferida e independiente del baseline GET. **Finaliza cuando:** el Creador posee un adapter/hook feature-local de evaluación que comparte el parser puro de respuesta efectiva con GET, envía sólo `{ values: ResourceAttribute[] }` y representa CONTROLLED_OPTION exactamente, sin confirmar creación ni ejecutar evaluación local. **Rollback:** revertir el adapter/hook de Resources Master y la extracción compartida junto con sus regresiones GET/POST; conservar el baseline de Catálogo operativo.

- [ ] **RED → GREEN → TRIANGULATE → REFACTOR:** una vez confirmada la ruta pública y el contexto requerido sin inventar query/body, crear `tests/unit/resourceAttributeEvaluationApi.test.ts` y `tests/unit/useResourceAttributeEvaluation.test.tsx` para body exclusivo `{ values }`, `CONTROLLED_OPTION` como `{ kind: 'CONTROLLED_OPTION', value: selectedOptionCode }`, rechazo previo a POST de TEXT/ENUM/CODE para ese descriptor, error/red/schema, borrador/contexto stale, cancelación, retry manual y cero create/update/lifecycle; extraer `parseEffectiveAttributesResponse(unknown, expectedTypeCode)` y tipos compartidos a `src/shared/catalog/effectiveAttributes.contract.ts`, hacer que `catalogTypeEffectiveAttributes.api.ts` y el nuevo `src/features/resources-master/resourceAttributeEvaluation.api.ts` usen el mismo parser, implementar `useResourceAttributeEvaluation.ts` con identidad de contexto y revisión capturada del borrador, y ampliar `tests/architecture/restTransportBoundaries.test.ts` sólo a este quinto adapter; triangular que POST no usa actor por inferencia, no altera GET, no habilita creación/fingerprint/disposiciones ni evalúa rules/allowed-values localmente; si falta ruta pública exacta, registrar G4 y no enviar request, y ejecutar `pnpm test` y `pnpm typecheck`. <!-- sdd-owner: implementation -->

**Cierre limitado G3/G6:** las unidades 7a-i–7c cierran la lectura autoritativa de atributos efectivos de pantalla y Unidad 6 permanece como lectura lower-level; 7f deja la evaluación por valores explícitamente diferida y aislada. CRUD/lifecycle, `RELACION_OPCIONES`, allowed-values, `effectiveReasons`, `aggregateStatus`, violaciones, clasificación, fingerprint, disposiciones y creación continúan bloqueados. Ninguna unidad 6–7f envía mutaciones ni interpreta reglas.

### 8. Recursos: adapter REST de lista, detalle y DTO `ResourcePage` — 320–400 líneas

**Depende de:** unidades 1–2. **Finaliza cuando:** lista/búsqueda/detalle usan sólo rutas públicas, filtros por códigos y DTOs REST, sin cablear todavía jerarquía compartida ni pantalla. **Rollback:** revertir `resourcesMaster.api.ts`, `resourcesMaster.types.ts` y sus tests; no modificar hook/UI todavía.

- [x] **RED → GREEN → TRIANGULATE → REFACTOR:** reemplazar primero en `tests/unit/resourcesMasterApi.test.ts` los casos Convex por fallos de URL codificada, query `scope/text/classCode/familyCode/typeCode/limit/offset`, `ResourcePage`, detalle identificable, descripción sólo con ruta confirmada, valores `CatalogValue` completos, strings de id/revisión y `{ error }`; implementar los parsers/mappers REST en `src/features/resources-master/resourcesMaster.api.ts` y `resourcesMaster.types.ts`; triangular JSON inválido, 400/404/409/422/500/503, red y abort sin falso vacío/éxito; refactorizar sin Zod fuera del adapter y ejecutar `pnpm test`. <!-- sdd-owner: implementation -->

### 9. Creador keyboard-first: bloqueo temporal G4/G5 y desconexión Convex — 280–370 líneas

**Depende de:** unidad 8 y coordinación con `keyboard-first-resource-creation`. **Finaliza cuando:** el shell conserva foco, volver/cerrar y nombre accesible, mientras evaluación, fingerprint, valores permitidos, políticas, unidad natural y creación quedan bloqueados temporalmente; sus hooks/conexiones Convex sin consumidores se retiran antes de migrar jerarquía compartida. **Rollback:** revertir sólo `CrearRecursoSurface.tsx`, `ResourceCreationContractPending.tsx`, hooks/modelos legacy tocados y sus pruebas.

- [x] **RED → GREEN → TRIANGULATE → REFACTOR:** convertir `tests/unit/useResourceCreationFlow.attributes.test.tsx`, `tests/unit/useResourceCreationEvaluation.test.tsx`, `tests/unit/useResourceCreationAllowedValues.test.tsx`, `tests/unit/resourceCreation.activeUnits.test.ts`, `tests/unit/resourceCreation.loaders.test.ts` y `tests/unit/resourceCreationReview.test.tsx` en pruebas rojas de bloqueo sin llamadas para evaluación/fingerprint/valores permitidos/políticas y `UNIDAD → naturalUnit`; adaptar `CrearRecursoSurface.tsx`, `ResourceCreationContractPending.tsx`, `useResourceCreationFlow.ts`, `resourceCreation.loaders.ts`, `resourceCreation.activeUnits.ts` y consumidores para retirar las conexiones Convex sin consumidores; triangular navegación teclado, volver/cerrar/Escape, foco restaurado y cambio de contexto; refactorizar sin listener global, CSS arbitrario ni suspensión/restauración por reglas locales, y ejecutar `pnpm test`. <!-- sdd-owner: implementation -->
- [x] Registrar la decisión humana de bloqueo temporal G4/G5 en `openspec/changes/replace-convex-with-rest-backend/gap-reports/G4.md` y `G5.md`, con paso keyboard-first, evidencia pública, impacto y conexión retirada; no solicitar otra decisión ni habilitar POST como atajo o inferir unidad por id/código/símbolo. <!-- sdd-owner: implementation -->

### 10. Recursos: jerarquía REST por códigos — 260–350 líneas

**Depende de:** unidad 9. **Finaliza cuando:** las lecturas de Clase, Familia y Tipo para Recursos usan `GET` sobre `/v1/catalog/CLASE` (read-only), `/v1/catalog/FAMILIA` (read-only) con `classCode`, y `/v1/catalog/TIPO` (read-only) con `familyCode`; validan referencias y limpian descendientes al cambiar el padre. **Rollback:** revertir `resourcesMaster.api.ts`, `useResourcesHierarchy.ts`, tipos y sus pruebas; no cablear pantalla/lista aún.

- [x] **RED → GREEN → TRIANGULATE → REFACTOR:** crear primero casos rojos en `tests/unit/resourcesMasterApi.test.ts` y `tests/unit/useResourcesHierarchy.test.ts` para queries por código, referencias `class`/`family` coincidentes, rechazo previo a React, limpieza de descendientes/offset al cambiar Clase o Familia y ausencia de cursor sintético; implementar mappers y lecturas feature-locales en `resourcesMaster.api.ts`, `resourcesMaster.types.ts` y `useResourcesHierarchy.ts`; triangular referencia ajena, respuesta stale, vacío/error y flags de ventana sin prometer orden G2; refactorizar sin colección global ni política/unidad inferida y ejecutar `pnpm test`. <!-- sdd-owner: implementation -->

### 11. Recursos: módulo REST aislado de ventana — 320–390 líneas

**Depende de:** unidad 10. **Finaliza cuando:** un módulo nuevo `useResourcesMasterRestWindow.ts` conserva una sola ventana REST por identidad de texto/scope/códigos/limit/offset, debounce de 250 ms, retry manual y cache aislada, sin conectarse todavía a la pantalla ni modificar los hooks Convex legacy. **Rollback:** revertir sólo `src/features/resources-master/useResourcesMasterRestWindow.ts`, `tests/unit/useResourcesMasterRestWindow.test.tsx` y cualquier helper de test nuevo de esta unidad; conservar intactos `useResourcesMasterListQuery.ts`, `useResourcesMasterList.ts`, sus pruebas y `ResourcesMasterScreen.tsx`.

**Rediseño por etapas registrado:** el intento in-place previo en la unidad 11 produjo fallo de `pnpm typecheck` y un diff de 1.155 líneas; esta unidad crea un camino REST aislado y compilable, sin proyección de compatibilidad ni selector/fallback runtime. La coexistencia de archivos legacy y REST es sólo transitoria hasta el switch atómico de la unidad 12 y su retirada explícita en la unidad 14.

- [x] **RED → GREEN → TRIANGULATE → REFACTOR:** crear primero `tests/unit/useResourcesMasterRestWindow.test.tsx` como casos rojos del módulo nuevo para offset reset, debounce, cache aislada por montaje, flags `hasPrevious/hasNext`, navegación pendiente, retry explícito, identidad por texto/scope/todos los códigos/limit/offset y respuesta stale; implementar exclusivamente `src/features/resources-master/useResourcesMasterRestWindow.ts` con `useQuery`, `retry:false` y el adapter REST de `resourcesMaster.api.ts`, sin refetch automático, sin wiring de pantalla y sin importar/modificar `useResourcesMasterListQuery.ts` ni `useResourcesMasterList.ts`; triangular error inicial/de navegación, respuesta antigua y `refetchActive()` de la identidad vigente; refactorizar sólo duplicación interna del módulo, sin flatten/dedupe/«Cargar más…», y ejecutar `pnpm test` y `pnpm typecheck`. <!-- sdd-owner: implementation -->

### 11a. Remediación residual: Creador bloqueado sin API ni snapshot — 150–230 líneas

**Depende de:** unidad 9 completada y coordinación con `keyboard-first-resource-creation`. **Estado:** remediación posterior a la unidad 9; no altera ni desmarca su registro histórico de G4/G5. **Finaliza cuando:** `CrearRecursoSurface` abre directamente el estado accesible `ResourceCreationContractPending` sin requerir `ResourcesMasterApi`, `ownership` ni `InitialResourceHierarchySnapshot`, y no monta flujo, jerarquía ni snapshot al abrir. El trigger humano aprobado, el foco inicial/restaurado, volver/cerrar y Escape se conservan; abrir, navegar y cerrar emiten cero llamadas Convex y cero HTTP REST. **Rollback:** revertir sólo `src/features/resources-master/CrearRecursoSurface.tsx`, su prop/caller residual en `src/features/resources-master/ResourcesMasterScreen.tsx` y las pruebas de esta remediación; no revertir ni reactivar conexiones legacy retiradas por la unidad 9.

- [x] **RED → GREEN → TRIANGULATE → REFACTOR:** crear `tests/unit/crearRecursoSurfaceContractPending.test.tsx` con casos rojos que monten el Creador desde su caller de `ResourcesMasterScreen.tsx` sin `ResourcesMasterApi`, ownership ni snapshot y comprueben que activar «Nuevo recurso»/N enfoca y anuncia `ResourceCreationContractPending`, que Volver, cerrar y Escape conservan los contratos de foco y cierre, y que spies de `fetch` y de las rutas Convex no reciben llamadas; eliminar de `CrearRecursoSurface.tsx` los props/imports de API, ownership, snapshot y `useResourceCreationFlow`, ajustar exclusivamente su caller residual en `ResourcesMasterScreen.tsx` y conservar `Dialog`, `CreationStageRail`, `ResourceCreationShell` y `CreationCommandBar` sin CSS nuevo; triangular apertura repetida, cierre por diálogo y restauración al opener, refactorizar sin listener global ni fallback, y ejecutar `pnpm test` y `pnpm typecheck`. <!-- sdd-owner: implementation -->

### 12a. Extensión compartida de `HierarchyNavigator` para ventanas offset — 180–260 líneas

**Depende de:** unidades 10–11. **Finaliza cuando:** `src/shared/ui/HierarchyNavigator.tsx` expone controles accesibles de anterior/siguiente para una ventana offset mediante callbacks y flags recibidos, sin importar hooks ni adapters de Recursos, sin cursor/«Cargar más…»/dedupe y sin modificar CSS o tokens. **Rollback:** revertir sólo `src/shared/ui/HierarchyNavigator.tsx` y sus pruebas; no tocar `ResourcesMasterScreen.tsx` ni el wiring REST.

- [x] **RED → GREEN → TRIANGULATE → REFACTOR:** crear `tests/unit/hierarchyNavigator.test.tsx` con casos rojos para anterior/siguiente visibles y con nombre accesible sólo cuando `hasPrevious`/`hasNext` lo permiten, callbacks correctos, controles no accionables durante navegación pendiente y conservación de selección, estado inicial/error/reintento y metadatos espaciales existentes; extender las interfaces de `src/shared/ui/HierarchyNavigator.tsx` con el contrato mínimo de ventana offset y renderizar los controles reutilizando el componente y sus clases actuales, sin hooks REST, CSS, cursores ni «Cargar más…»; triangular cada combinación de flags y pending, refactorizar sólo la duplicación local y ejecutar `pnpm test` y `pnpm typecheck`. <!-- sdd-owner: implementation -->

### 12b. Recursos: switch atómico de pantalla a REST y proyección G8 — 300–390 líneas objetivo

**Depende de:** unidades 8, 10, 11, 11a y 12a. **Gate de tamaño:** reestimar código, pruebas y fixtures con `git diff --stat` antes de apply; si el forecast medido supera 400 líneas, detener esta unidad bajo `ask-on-risk` para decisión humana, sin absorber 12a ni reclamar `size:exception`. **Finaliza cuando:** un único corte compilable conecta `ResourcesMasterScreen.tsx`, `useResourcesHierarchy.ts`, `useResourcesMasterRestWindow.ts` y los controles offset ya compartidos, y proyecta exclusivamente campos REST reales con copy accesible; no presenta nombre, ownership, `classificationStatus` ni diagnósticos Convex. Los hooks Convex legacy pueden coexistir sin consumidores sólo hasta la limpieza de la unidad 14; no existe selector ni fallback runtime. **Rollback:** revertir conjuntamente `ResourcesMasterScreen.tsx`, su wiring REST, tipos de proyección, fixtures estrictamente necesarias y pruebas de pantalla de este corte, sin revertir adapter, jerarquía, módulo REST aislado ni 12a.

- [x] **RED → GREEN → TRIANGULATE → REFACTOR:** fijar primero casos rojos en `tests/unit/resourcesMasterScreen.test.tsx` y `tests/unit/resourcesMasterScreenRefetch.test.tsx` para carga/vacío/error, filtros jerárquicos por código, anterior/siguiente offset G2, foco/teclado, refetch sólo de identidad activa, Creador ya desacoplado y ausencia de campos legacy; si las pruebas existentes requieren doble, añadir sólo un fixture REST local a esos archivos o `tests/unit/resourcesMasterScreenRestFixture.tsx`, sin importar `useResourcesMasterListQuery.ts`/`useResourcesMasterList.ts` ni crear proyección de compatibilidad; cablear atómicamente `ResourcesMasterScreen.tsx` a `useResourcesHierarchy.ts`, `useResourcesMasterRestWindow.ts` y la extensión de `HierarchyNavigator`, adaptar `resourcesMaster.types.ts` a campos REST verdaderos y retirar del árbol de consumo de la pantalla `useResourcesMasterListQuery.ts`/`useResourcesMasterList.ts`; triangular recurso sin descripción, 404 de detalle y resultado stale; refactorizar preservando `PageHeader` y `WorkCard`, y ejecutar `pnpm test` y `pnpm typecheck`. <!-- sdd-owner: implementation -->
- [x] Registrar separadamente la adaptación G8 ya autorizada en `openspec/changes/replace-convex-with-rest-backend/gap-reports/G8.md`, con columnas/detalle retirados, evidencia pública e impacto visible; no requerir aceptación humana adicional ni inventar campos de reemplazo. <!-- sdd-owner: implementation -->

### 13. Recursos: mutaciones REST aprobadas y fail-closed — 300–390 líneas

**Depende de:** unidades 2, 8, 9 y 12; el Creador bloqueado no autoriza una creación UI equivalente. **Finaliza cuando:** create/update/deactivate/reactivate sólo tienen adapter y UI consumidora cuando su entrada concreta está auditada; actor y `expectedRevision` permanecen strings, sin reintento automático. **Rollback:** revertir `resourcesMaster.api.ts`, hooks de mutación explícitamente conectados y tests de este corte; no revertir ni tocar datos backend.

- [ ] **RED → GREEN → TRIANGULATE → REFACTOR:** añadir casos rojos a `tests/unit/resourcesMasterApi.test.ts` y `tests/unit/useResourceCreationCreate.test.tsx` para bodies públicos de POST/PUT/lifecycle, actor fail-closed incluso por llamada directa, revisión string, 201 `Resource` válido, 409 sin reenvío, 422/503/red y doble envío; implementar en `resourcesMaster.api.ts` y sólo en hooks consumidores auditados las mutaciones REST, manteniendo `useResourceCreationCreate.ts` bloqueado para el UX actual por G4/G5; triangular respuesta 201 inválida, contexto cambiado y refetch de identidad activa; refactorizar sin optimistic update, idempotencia, backoff o test conectado mutante y ejecutar `pnpm test`. <!-- sdd-owner: implementation -->
- [ ] Cuando una mutación visible no pueda habilitarse por G4/G5/G6, ampliar el registro de decisión o gate correspondiente y dejar el control con explicación accesible; no reemplazarlo por fixture, persistencia local, Convex ni request de sondeo. <!-- sdd-owner: implementation -->

### 14. Limpieza diferida de Convex y regresión REST-only — 300–390 líneas

**Depende de:** todas las unidades anteriores —incluidos el switch atómico de la unidad 12 y las unidades 6, 7a-i–7c-i—, decisión humana que mantenga explícitamente diferidas las bases opcionales 7d–7e y, para 7f, implementación aceptada o el bloqueo G4 registrado. **Finaliza cuando:** cada consumidor accesible usa REST o tiene bloqueo/decisión aceptada; sólo entonces desaparecen dependencia, lockfile, imports, config, RPC, pruebas conectadas, excepciones Convex y los módulos/hook/tests legacy de la lista de Recursos. **Rollback:** revertir la entrega frontend final completa; no crear un runtime dual ni afirmar que un rollback deshace escrituras backend.

- [ ] **RED → GREEN → TRIANGULATE → REFACTOR:** primero hacer fallar `tests/architecture/queryZodBoundaries.test.ts`, `tests/architecture/catalogHierarchyBoundaries.test.ts`, `tests/architecture/runtimeFixtureIsolation.test.ts`, `tests/integration/catalogHierarchyConnectedTransport.test.ts` y `tests/integration/activeUnitResourceCreation.connected.test.ts` al exigir cero imports/URLs/RPC/tests Convex y HTTP sólo en los cuatro adapters GET autorizados, o en cinco si 7f fue aceptada con su guarda exacta; añadir la guarda que detecte consumidores residuales de `src/features/resources-master/useResourcesMasterListQuery.ts`, `src/features/resources-master/useResourcesMasterList.ts` y, tras el switch efectivo 7c confirmado, `src/features/catalog-hierarchy/catalogTypeAttributes.api.ts`; después de verificar que el switch de la unidad 12 no dejó consumidores, eliminar atómicamente esos módulos/hook legacy y sus pruebas, sustituir o retirar las demás pruebas conectadas legacy por dobles REST y guardas AST de `fetch`, imports dinámicos/reexports y bundle; eliminar Convex de los cuatro adapters GET autorizados y del quinto de evaluación sólo si 7f fue aceptada, `package.json`, `pnpm-lock.yaml`, variables/config y código/pruebas sin consumidores; triangular una infracción por cada ruta prohibida y ausencia de actor; refactorizar guardas para que no preserven una excepción Convex y ejecutar `pnpm test` y `pnpm typecheck`. <!-- sdd-owner: implementation -->
- [ ] Ejecutar y registrar, tras la suite verde, `pnpm typecheck`, `pnpm lint`, `pnpm format:check`, `pnpm build`, `pnpm verify:runtime-bundle`, `pnpm test:stories` y los escenarios aplicables de `pnpm test:e2e`; corregir sólo fallos de esta change y mantener pruebas con su unidad causal. <!-- sdd-owner: implementation -->
- [ ] Después de que el proxy y las lecturas unitarias estén verdes, realizar un smoke **de lectura pública** desde navegador mediante `/v1` (read-only) en `tests/e2e/resourcesMaster.workstation.spec.ts` y los escenarios REST de catálogo aplicables: verificar carga/error accesible y que el proxy no demuestra CORS/producción; no ejecutar POST/PUT/deactivate/reactivate ni ninguna prueba conectada mutante sin autorización humana explícita. <!-- sdd-owner: implementation -->

## Acciones del parent y gates de ciclo de vida

- [x] Antes de apply, presentar este forecast alto, el alcance reducido y el orden de slices al humano. Completado: se presentó el forecast entonces vigente de 2.700–3.500 líneas y el humano seleccionó entrega encadenada `auto-chain` con `stacked-to-main`. <!-- sdd-owner: parent -->
- [ ] Tras 7a-ii, confirmar el cierre parent de la partición del candidato verde de 611 líneas: receipts de pruebas/guardas/calidad registrados, cada diff <=400 y ningún cambio de producción antes de habilitar 7b. <!-- sdd-owner: parent -->
- [ ] Antes de cerrar cada slice afectado, revisar los reportes G1–G8 aplicables; aplicar G4, G5 y G8 conforme a las decisiones ya registradas y obtener decisión humana sólo para una brecha aún no resuelta o contrato público adicional. <!-- sdd-owner: parent -->
- [x] Antes de aplicar 7c-i, confirmar su preflight de código y pruebas; si supera 400 líneas, pausar bajo `ask-on-risk` y pedir división lista→detalle antes de editar. Completado: el forecast y la medición final de la unidad, incluido el ajuste Dialog aprobado, permanecen dentro del techo autorizado de 900 líneas. <!-- sdd-owner: parent -->
- [ ] Antes de aplicar 12b, revisar su forecast medido incluyendo código, pruebas y fixtures; si supera 400 líneas, pausar bajo `ask-on-risk` para pedir una decisión humana de entrega en vez de mezclar 12a o asumir una excepción de tamaño. <!-- sdd-owner: parent -->
- [ ] Antes de la retirada final, comprobar que todos los consumidores accesibles están REST o bloqueados con decisión humana aceptada, que el diff de cada unidad conserva pruebas/código juntos y que no hay conflicto sin resolver con `catalog-hierarchy-base` o `keyboard-first-resource-creation`. <!-- sdd-owner: parent -->
