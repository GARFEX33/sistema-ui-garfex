# Feature: Maestro de Proveedores

## Objetivo

Página y CRUD del Maestro de Proveedores, mismo patrón funcional/visual que Recursos maestros (`src/features/resources-master`).

## Problema / por qué

El backend (`garfex-api`, REST, `/v1`) ya expone el dominio de proveedores. El frontend no tiene ningún feature para administrarlo.

## Decisión de alcance (usuario, 2026-09-18)

El design brief (`docs/erp-first-stage-design-brief.md:43`) excluye explícitamente "flujos completos de compras y proveedores" de esta primera etapa. El usuario confirmó que lo que se construye acá es **solo el maestro de Proveedores** (CRUD de datos maestros — nombre, identificador fiscal, contacto, etc.), análogo a Recursos maestros, **sin** flujos de compra/orden de compra. Es una ampliación acotada de scope, no lo que el brief excluye.

## Contrato de backend (verificado en vivo, `GET http://localhost:8090/openapi.yaml`, caja negra — nunca leer código de `garfex-api`)

- `GET /v1/suppliers` — búsqueda paginada (`scope: ACTIVE|INACTIVE|ALL`, `text`, `limit` ≤50, `offset`) → `SupplierPage { suppliers[], hasPrevious, hasNext }`.
- `POST /v1/suppliers` — crea (`SupplierCreateRequest { actor(required), tradeName, legalName, taxIdentifier, website, notes }`) → `Supplier` (201).
- `GET /v1/suppliers/{id}` — detalle.
- `PUT /v1/suppliers/{id}` — reemplazo completo (`SupplierUpdateRequest`, mismos campos que create; **sin** `expectedRevision` — a diferencia de Resource, Supplier no usa concurrencia optimista).
- `Supplier { id, tradeName, legalName, taxIdentifier, website, notes, active, createdAt, updatedAt }`.
- `GET /v1/suppliers/{id}/branches`, `GET /v1/suppliers/{id}/branches/{branchId}` — solo lectura (`Branch`).
- `GET /v1/suppliers/{id}/contacts`, `GET /v1/suppliers/{id}/contacts/{contactId}` — solo lectura (`Contact`).

**Restricción real, no negociable en esta iteración:** no existe `deactivate`/`reactivate`/`delete` para Supplier (a diferencia de `catalog` y `resources`, que sí los tienen). No existe `POST`/`PUT` para branches/contacts. El CRUD posible es **Crear, Listar/Buscar, Editar** únicamente. No prometer baja lógica ni edición de branches/contacts hasta que el backend la exponga.

## Decisiones de UI (siguiendo `garfex-design-system`)

- Reusar `PageHeader`, `WorkCard`, `Dialog`, `Field`, `Button` de `src/shared/ui/` — no crear equivalentes locales.
- Mismo patrón visual/estructural que `ResourcesMasterScreen` (lista + búsqueda + card) para la lista; mismo patrón de diálogo modal que las creaciones de catálogo (form flat, no wizard — el dominio Supplier no tiene jerarquía) para crear/editar.
- Nuevo link de navegación de nivel superior "Proveedores" en `AppShell`, análogo a "Recursos maestros" (no anidado bajo el placeholder estático "Compras", que sigue fuera de alcance).
- Nueva superficie de teclado (`activeSurface: 'proveedores'`) registrada en el subsistema de teclado (`src/shared/keyboard/`), Tab/Escape/spatial-nav consistentes con las pantallas existentes.

## Cambios de arquitectura deliberados (no workarounds — declarar explícitamente)

- Agregar `src/features/proveedores/proveedores.api.ts` al allowlist de `tests/architecture/restTransportBoundaries.test.ts` **y** de `tests/architecture/keyboardBoundaries.test.ts` (mismo allowlist de adapters REST, duplicado en ese archivo); agregar `proveedores.api.ts` (zod) y `useProveedoresRestWindow.ts` (`useQuery`) al allowlist de `tests/architecture/queryZodBoundaries.test.ts`.
- `tests/architecture/catalogHierarchyBoundaries.test.ts` fija `expect(shell.match(/<Link/g)).toHaveLength(3)` — subirá a 4 al agregar el link de Proveedores; actualizar el test deliberadamente.
- Revisar si el tipo de unión de `activeSurface` en el subsistema de teclado necesita el nuevo valor `'proveedores'`.

## TDD

Strict TDD activo (`openspec/config.yaml` + system config). RED → GREEN → REFACTOR por tarea. Runner: Vitest (`pnpm exec vitest run <archivo>`).

## Tareas

- [x] **T1 — Tipos + adapter REST** (`proveedores.types.ts`, `proveedores.api.ts`): parseo/validación de `Supplier`/`SupplierPage`, `listSuppliers`, `createSupplier`, `getSupplier`, `updateSupplier`. Allowlist de arquitectura actualizado. Tests unitarios del adapter.
- [x] **T2 — Lista/búsqueda** (`useProveedoresRestWindow.ts`, `ProveedoresScreen.tsx`, `ProveedoresEntry.tsx`): ventana paginada mirror de `useResourcesMasterRestWindow`, búsqueda por texto, `PageHeader`+`WorkCard`, spatial-nav en filas.
- [x] **T3 — Crear proveedor** (`CrearProveedorSurface.tsx`): diálogo modal, validación mínima, refresco de lista tras crear (cuidado: bug conocido en Recursos fue que la lista no se refrescaba tras crear — verificar invalidación real).
- [x] **T4 — Editar proveedor** (`EditarProveedorSurface.tsx`): diálogo precargado, `PUT` sin `expectedRevision`.
- [x] **T5 — Ruteo + navegación + teclado** (`src/app/routes/proveedores.tsx`, `AppShell.tsx`, subsistema de teclado, actualización deliberada de los tests de arquitectura afectados) + smoke e2e.

## Fuera de alcance explícito (no asumir, no implementar)

- Baja/reactivación de proveedores (backend no lo soporta).
- CRUD de branches/contacts (backend es solo lectura para ellos). Vista de solo lectura de branches/contacts queda como trabajo futuro, no parte de estas tareas.
- Cualquier flujo de compras/órdenes de compra.

## Progreso

(se actualiza tarea por tarea con evidencia de tests, typecheck/lint, y commit)

- **T1** (2026-09-18): `proveedores.types.ts` + `proveedores.api.ts` creados (RED→GREEN); `tests/unit/proveedoresApi.test.ts` — 16/16 passed; `tests/architecture/restTransportBoundaries.test.ts` — 5/5 passed tras agregar el adapter al allowlist; `pnpm typecheck` y `pnpm lint` sin errores.
- **T2** (2026-09-18): `useProveedoresRestWindow.ts` (mirror de `useResourcesMasterRestWindow`, texto enviado al backend sin bug documentado a diferencia de Recursos) + `ProveedoresScreen.tsx` (header+card+tabla, sin acción de creación, atributos `data-spatial-id="proveedores.search"` / `data-spatial-id="proveedores.<id>"` / `data-supplier-row` para spatial-nav futura) + `ProveedoresEntry.tsx` creados (RED→GREEN, RED confirmado moviendo temporalmente cada archivo nuevo y viendo fallar el import antes de restaurarlo). `tests/unit/useProveedoresRestWindow.test.tsx` — 6/6 passed; `tests/unit/proveedoresScreen.test.tsx` — 10/10 passed; `tests/unit/proveedoresApi.test.ts` — 16/16 passed (sin tocar T1); `tests/architecture/restTransportBoundaries.test.ts` — 5/5 passed; `pnpm typecheck` y `pnpm lint` sin errores. No se creó ruta ni se tocó `AppShell.tsx`/`src/shared/keyboard/` (T5).
- **T3** (2026-09-18): `CrearProveedorSurface.tsx` (diálogo flat, mismo patrón que `NuevaClaseSurface`/`CrearAtributoSurface`, no el wizard de Recursos) + trigger cableado en `ProveedoresScreen.tsx`, refresco de lista tras crear vía `refetchActive` (evita el bug documentado de Recursos). `tests/unit/proveedoresScreen.test.tsx` — 11/11 passed; `tests/unit/crearProveedorSurface.test.tsx` — 7/7 passed; `tests/unit/useProveedoresRestWindow.test.tsx` — 6/6; `tests/unit/proveedoresApi.test.ts` — 16/16; `pnpm typecheck`/`pnpm lint` sin errores.
- **Hotfix post-T3** (2026-09-18, orquestador): T1/T2 habían dejado sin actualizar dos allowlists de arquitectura adicionales que no estaban en la lista de "Cambios de arquitectura deliberados" original — `tests/architecture/queryZodBoundaries.test.ts` (zod en `proveedores.api.ts`, `useQuery` en `useProveedoresRestWindow.ts`) y `tests/architecture/keyboardBoundaries.test.ts` (duplica el allowlist de adapters REST). Corregido y verificado: `pnpm exec vitest run tests/architecture` — 7/7 archivos, 42/42 tests passed.
- **T5** (2026-09-18): `src/app/routes/proveedores.tsx` creado (mirror exacto de `recursos.tsx`, componente `ProveedoresEntry`). `AppShell.tsx`: nuevo link top-level "Proveedores" (`data-spatial-id="sidebar.proveedores"`) insertado entre "Recursos maestros" y el placeholder estático "Compras" (mismo grupo `ESPACIOS DE TRABAJO`, sin anidar bajo "Compras"), renumerando `sidebarLinks.current` (0 Bandeja, 1 Recursos maestros, 2 Proveedores, 3 Catálogo) y el índice pasado a `handleSidebarKeyDown` del link de Catálogo (2→3); `activeSurface` y el label de `topbar-route` ganan la rama `'/proveedores'`; `ArrowRight` desde `sidebar.proveedores` enfoca `proveedores.search`; `ArrowLeft`/`Escape` desde `proveedores.search` regresan el foco a `sidebar.proveedores` (Proveedores no tiene columnas de jerarquía como Recursos, así que no hay `focusDeepestResourcesHierarchy` equivalente — vuelve directo al link). `openHelp`'s surface param y `KeyboardSurface` (`src/shared/keyboard/keyboardControllerContext.ts`) ganan el literal `'proveedores'`; revisado `KeyboardHelpDialog.tsx` y `CommandEntry.tsx` — ambos filtran comandos genéricamente por `command.surface === surface` sin mapa de contenido por superficie que necesitara una entrada dedicada, así que no requieren cambios (el único condicional de texto es `surface === 'catalog' ? 'Catálogo' : 'Contexto actual'`, que ya cubre `'proveedores'` con el valor por defecto). Cambios de arquitectura deliberados: `tests/architecture/catalogHierarchyBoundaries.test.ts` — `expect(shell.match(/<Link/g)).toHaveLength(3)` → `4`; revisado el resto de `tests/architecture/` (`rg -n "recursos|bandeja|catalogo|proveedores"`) y no se encontró ningún otro allowlist de rutas/links/superficies hardcodeado además del ya cubierto por T1–T4 (adapters REST/zod/keyboard de `proveedores.api.ts` y `useProveedoresRestWindow.ts`, ya presentes desde el hotfix post-T3) — nada nuevo fuera de lo que el archivo de tareas anticipaba. `tests/unit/appShell.test.tsx` actualizado: los tres `toHaveLength(3)` de conteo de links → `4`; el test de navegación por teclado del sidebar renombrado y extendido para el grupo de cuatro links (Bandeja↔Recursos maestros↔Proveedores↔Catálogo, Home/End); nuevo test "resolves /proveedores..." (mirror del de `/catalogo`); nuevo test de `ArrowRight`/`ArrowLeft`/`Escape` entre `sidebar.proveedores` y `proveedores.search`. RED confirmado con `git stash` temporal de `AppShell.tsx` + `keyboardControllerContext.ts` (5/28 tests de `appShell.test.tsx` fallando: link no encontrado, conteos de 3 links, `aria-current` ausente, foco no cableado) antes de restaurar (`git stash pop`) y llegar a GREEN. Resultados: `tests/unit/appShell.test.tsx` — 28/28 passed; `tests/architecture` — 8 archivos, 46/46 tests passed (7 archivos/42 tests previos + `catalogHierarchyBoundaries.test.ts` con la nueva longitud); `tests/unit/proveedoresApi.test.ts` 16/16, `useProveedoresRestWindow.test.tsx` 6/6, `proveedoresScreen.test.tsx` 13/13, `crearProveedorSurface.test.tsx` 7/7, `editarProveedorSurface.test.tsx` 7/7 (los cinco sin tocar, corridos junto al resto para confirmar que T5 no rompió nada). `pnpm typecheck`, `pnpm lint`, `pnpm router:check` sin errores (el watcher del `pnpm dev` en curso ya había regenerado `routeTree.gen.ts` con la nueva ruta al crear `proveedores.tsx`; `router:generate`/`router:check` lo confirman limpio). E2E: `tests/e2e/proveedores.workstation.spec.ts` creado (mirror de `operationsInbox.workstation.spec.ts`, la spec más corta) — mockea `GET /v1/suppliers**` con página vacía vía `page.route` (mismo patrón de `catalogHierarchy.workstation.spec.ts`, no depende de un backend real en `localhost:8090`) y cubre: navegación real haciendo click en el link "Proveedores" del sidebar desde `/bandeja`, visibilidad del header/`aria-current`/buscador/tabla/estado vacío, y un chequeo axe sin variantes responsive. Ejecutado con `pnpm exec playwright test tests/e2e/proveedores.workstation.spec.ts` — 2/2 passed (se corrigió una violación de "strict mode" de Playwright agregando `exact: true` al heading "Proveedores", que por defecto matcheaba también "Listado de proveedores"). No se ejecutó la suite e2e completa (las otras specs son extensas y no era necesario para verificar T5). No se tocaron `create`/`edit` en el smoke e2e (ya cubiertos por unit tests de T3/T4).

- **T4** (2026-09-18): `EditarProveedorSurface.tsx` creado (RED→GREEN, RED confirmado moviendo temporalmente el archivo del componente y viendo fallar el import de `tests/unit/editarProveedorSurface.test.tsx` antes de restaurarlo) mirror exacto del patrón flat de `CrearProveedorSurface.tsx` (Dialog + `isDismissable` + Escape vía react-aria + `restoreFocusNextFrame`), precargando el draft desde la prop `supplier` en `open()` en vez de partir vacío, y llamando `updateSupplier` en vez de `createSupplier`. Sin `expectedRevision`/token de concurrencia en ningún punto (confirmado con test dedicado) — el contrato `PUT /v1/suppliers/{id}` no lo tiene, a diferencia de Resource. Trigger "Editar" por fila (no en el `PageHeader`, a diferencia de la creación) cableado en `ProveedoresScreen.tsx` dentro de una nueva columna "Acciones", con `aria-label="Editar ${tradeName||legalName||id}"` para distinguir filas (mismo patrón que `aria-label="Anterior proveedores"`); refresco de lista tras editar vía el mismo `refetchActive` de T3/T2. Tests: `tests/unit/editarProveedorSurface.test.tsx` — 7/7 passed (pre-carga de los 5 campos, submit con id+campos editados sin `expectedRevision`/`revision`, refresco+cierre, error de API sin cerrar, error específico de actor no configurado, cancelar y Escape cierran sin submitir y restauran foco). `tests/unit/proveedoresScreen.test.tsx` — 13/13 passed (11 previos + 2 nuevos: el trigger de una fila abre el diálogo precargado con los valores de esa fila y no de otra fila del listado, y una edición exitosa llama a `updateSupplier`+`refetchActive`, cierra el diálogo, y — simulando el efecto ya probado de `refetchActive` en `useProveedoresRestWindow.test.tsx` mediante un `rerender` con el hook mockeado devolviendo el proveedor actualizado — la fila visible muestra el valor nuevo y ya no el viejo, sin ninguna acción manual de recarga). `tests/unit/proveedoresApi.test.ts` — 16/16; `tests/unit/useProveedoresRestWindow.test.tsx` — 6/6; `tests/unit/crearProveedorSurface.test.tsx` — 7/7 (los tres sin tocar). `tests/architecture` — 7/7 archivos, 42/42 tests passed sin necesidad de tocar ningún allowlist (no se agregó ningún adapter/hook nuevo ni import de paquete protegido). `pnpm typecheck` y `pnpm lint` sin errores. No se tocó `src/app/routes/`, `AppShell.tsx` ni `src/shared/keyboard/` (T5).
- **T3** (2026-09-18): `CrearProveedorSurface.tsx` creado (RED→GREEN, RED confirmado corriendo `tests/unit/crearProveedorSurface.test.tsx` contra el componente inexistente antes de implementarlo) mirror del patrón flat de `NuevaClaseSurface.tsx`/`CrearAtributoSurface.tsx` (Dialog + `isDismissable` + Escape vía react-aria + `restoreFocusNextFrame`), sin registrar comando en el subsistema de teclado (fuera de alcance de T3). `createSupplier` resuelve el actor internamente (`withRestActor`), la superficie solo traduce `RestActorConfigurationError` a un mensaje. Validación mínima cliente: requiere `tradeName` o `legalName` no vacíos (el backend no lo exige, ver comentario en el código). Trigger "Nuevo proveedor" cableado como `action` de `PageHeader` en `ProveedoresScreen.tsx`, éxito llama a `refetchActive()` del hook de T2 (mismo patrón que `ResourcesMasterScreen`/`CrearRecursoSurface`, evitando el bug conocido de no invalidar tras crear). Tests: `tests/unit/crearProveedorSurface.test.tsx` — 7/7 passed (render de campos, submit deshabilitado sin nombre, submit con trim + refresco + cierre, error de API sin cerrar, error específico de actor no configurado, cancelar y Escape cierran sin submitir y restauran foco al trigger). `tests/unit/proveedoresScreen.test.tsx` — 11/11 passed (10 previos + 1 nuevo: trigger abre el diálogo y una creación exitosa llama `refetchActive`). `tests/unit/proveedoresApi.test.ts` — 16/16 passed; `tests/unit/useProveedoresRestWindow.test.tsx` — 6/6 passed (ambos sin tocar). `pnpm typecheck` y `pnpm lint` sin errores. Nota: `tests/architecture/queryZodBoundaries.test.ts` y `tests/architecture/keyboardBoundaries.test.ts` fallan por un allowlist pendiente de T1/T2 (zod en `proveedores.api.ts`, `@tanstack/react-query` en `useProveedoresRestWindow.ts`) — confirmado preexistente (falla igual con los cambios de T3 quitados vía `git stash`), no introducido ni corregido por T3. No se tocó `src/app/routes/`, `AppShell.tsx` ni `src/shared/keyboard/` (T5).

## Cierre de la feature (2026-09-18)

T1–T5 completas. El Maestro de Proveedores queda alcanzable de extremo a extremo: link "Proveedores" en `AppShell` (grupo `ESPACIOS DE TRABAJO`, entre "Recursos maestros" y "Compras"), ruta `/proveedores` (`src/app/routes/proveedores.tsx` → `ProveedoresEntry` → `ProveedoresScreen`), superficie de teclado propia (`activeSurface: 'proveedores'`) con spatial-nav mínima consistente (ArrowRight sidebar→buscador, ArrowLeft/Escape buscador→sidebar) y ayuda de teclado (`?`) funcionando sin contenido específico adicional porque el diálogo de ayuda filtra genéricamente por superficie. CRUD real disponible: listar/buscar (T2), crear (T3), editar (T4) — los tres contra el backend REST real de `garfex-api` (`/v1/suppliers`), sin datos inventados ni mocks en runtime.

Fuera de alcance explícito, confirmado vigente sin cambios (el backend no expone estas operaciones y no se prometió ni implementó ninguna):
- Baja/reactivación de proveedores.
- CRUD de branches/contacts (solo lectura en el backend; ni siquiera la vista de solo lectura se implementó — queda como trabajo futuro).
- Cualquier flujo de compras/órdenes de compra.
