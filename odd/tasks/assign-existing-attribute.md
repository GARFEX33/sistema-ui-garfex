# Assign an existing attribute

## Outcome
Crear atributo → Usar existente now provides a clear safe sequence: initial ACTIVE list/live filter → visible selection → direct reuse configuration → current confirmation → `Agregar al Tipo`. Selected rows use semantic styling, visible `Seleccionada`, aria-pressed and a polite name/code/valueType summary. Existing mode focuses search; create mode keeps its advanced disclosure and code focus.
Search remains usable for inspection while global mutation blockers disable result selection and assignment with an adjacent exact reason. Fresh actor/data permits selection; duplicate disables only its row. Positive integer position, current confirmation, actor/context/freshness and resolved ledger are required. Source/selection/position/mode/identity changes invalidate confirmation. Trigger/footer accessibility descriptions use distinct IDs.
Assignment skips POST CARACTERISTICA and calls assignExisting exactly once, creating only active applicability/presentation with reference id "0". Existing stale response, duplicate, authoritative refresh and unresolved replay guards remain.

## Tasks
- [x] A1: Map REST search, identity and refresh contracts.
- [x] A2: Implement/verify ACTIVE search and assignment.
- [x] A3: Block/verify unresolved write replays.
- [x] A4: Fix/verify decoder, initial list and live filtering.
- [x] A5: Redesign/verify selection, configuration and add-to-Tipo UX.
- [ ] A6: Resolve global native review — deferred outside user-selected scope; not approved.

## Verification
Final A5 independent verifier mu4j9hl3-j-04ni:71 focused tests passed; TypeScript, ESLint with zero warnings and Prettier passed. Parent modal spot check17pass. No concrete blocker. A5 coverage is postimplementation; no strict-TDD RED claim. No live browser/explicit StrictMode verification.
A4 independent verification:68pass plus type/lint/format. Live backend and production adapter parse `colo`; Vite runs at localhost5174. A3 independent verification:91pass; actual RED12pass/2fail before its fix.

## Limitations
The user must visually confirm the redesigned modal in the live browser. No cross-session persistent unresolved ledger guarantee. No automatic mutation replay; recovery remains read-only Core reread. Broader full-suite failures were not rechecked.

## Native review boundary
Native review review-a79f205cf7da8b7a froze an older whole dirty workspace60 paths11798 lines, captured four lenses and remains correction_required. No correction plan, approval or acknowledgement. Its options findings are outside user-selected scope; preserve options/permanent deletion. Later source changes make its frozen evidence stale. Do not reset/recover/abandon or claim approval without explicit authority.

## Next step
Reload http://localhost:5174 and test: open Usar existente, select a nonduplicate row, enter a positive Posición, review configuration, confirm and press Agregar al Tipo. If blocked, the footer now states the exact cause. No commits, installs or backend writes were performed by agents.
